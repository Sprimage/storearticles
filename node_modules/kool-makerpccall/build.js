'use strict';

const { spawnSync } = require('child_process');
const path = require('path');

spawnSync('node', [ path.join(__dirname, 'node_modules', '.bin', 'webpack-cli') ], {
  stdio: 'inherit'
});
